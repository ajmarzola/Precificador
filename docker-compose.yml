version: '3.9'

services:
  precificador-api:
    build:
      context: .
      dockerfile: src/Precificador.WebApi/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

